function highlightProject(element) {
    element.style.animation = 'highlight 0.3s ease';
  }
  
  function resetProject(element) {
    element.style.animation = ''; // Reset animation after it completes
  }
  

